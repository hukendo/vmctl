usage: vmctl.py [-h] {list,run,pause,play,ssh} ...

Control VMware virtual machines.

positional arguments:
  {list,run,pause,play,ssh}

optional arguments:
  -h, --help            show this help message and exit
